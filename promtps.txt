I am working on a personal Webapp to use as a Daily Task Tracker.  Currently, it has a daily overview of tasks that should be done every day to keep track of the status of each task, and a monthly view of all the tasks for every day for the month.  This all works nicely because every day there are the exact same tasks to do.  I want to add some more functionality to this.  Basically, I want to add new functionality for customs tasks for the User to input themselves onto a totally separate page containing a totally separate list, and to be able to track when they have completed them.  I want to add a totally new page that is accessable from both the Daily Views and the Monthly Views.  This new page should be called the "ToDo List", so both the daily views and monthly views should have a button added towards the top that says "ToDo List" that navigates the user to this new page when clicked.  The ToDo List shall be a totally separate list of items from the daily tasks.  Initially, it shall start out empty.  The top of the ToDo list shall have a button to add a new item to the ToDo List.  Upon clicking the button, the User shall be prompted to enter in a name for the new ToDo List Item they are creating.  There should also be an "Add" button that appears next to the text entry for the User input so that the User can click it when they are done entering the ToDo list item.  Upon clicking the OK button, the new ToDo List Item shall be added to the ToDo List Page.  All items on the ToDo List Page shall contain a button that is labeled "Complete".  Upon clicking the "Complete" button, the ToDo List Item shall be moved to a separate list on the bottom of the ToDo List page that is labelled "Completed Items".  When it is added to this list, the item shall contain the original name entered by the User when they put it onto the ToDo List, and it shall have the date and time of when the User clicked the "Complete" button to complete the item.  I want the end result to look clean and professional, to support and be consistent with the light / dark mode theming that the other pages support, and to be cleverly designed.

The file structure for the webapp project is as follows:

personal-task-tracker/
├── app.py
├── requirements.txt
├── tasks.json
├── README.md
├── templates/
│   ├── base.html
│   ├── daily.html
│   └── monthly.html
└── static/
    └── css/
        └── style.css
    └── js/
        └── theme.js

Below is the contents for each of the files above:



FILE: app.py

#!/usr/bin/env python3
"""
Simple Flask application for tracking daily tasks.
Author: [Your Name]
"""

import os
import json
import datetime
from flask import Flask, render_template, request, redirect, url_for

app = Flask(__name__)

# Path to the JSON file storing task statuses
DATA_FILE = 'tasks.json'

# The fixed list of tasks for each day
TASKS = [
      'Wake Up By 8:30',
      'Log Weight',
      'Make Bed',
      'Morning Mantra',
      'Meditate',
      'Morning Sun',
      'Stretch',
      'Check Email',
      'Morning Hygine',
      'Vitamins/Creatine',
      'Workout Regime',
      'Feed & Take Out Sharron',
      'Work Goals',
      'Tidy Up',
      'ToDo (~30min)',
      'Music Production Goals',
      'Protein Shake',
      'Count Macros',
      'Meet Macros',
      'Close Rings',
      'Nighttime Hygine',
      'Nighttime Stretch',
      'Bed By Midnight'
]

def get_previous_month(year: int, month: int) -> tuple[int, int]:
    """Return (prev_year, prev_month)."""
    return (year - 1, 12) if month == 1 else (year, month - 1)

def get_next_month(year: int, month: int) -> tuple[int, int]:
    """Return (next_year, next_month)."""
    return (year + 1, 1) if month == 12 else (year, month + 1)

def load_data():
    """Load the tasks data from the JSON file."""
    if not os.path.exists(DATA_FILE):
        # Create an empty file if it doesn't exist
        with open(DATA_FILE, 'w') as f:
            json.dump({}, f)
        return {}
    with open(DATA_FILE, 'r') as f:
        try:
            data = json.load(f)
            # Ensure keys are strings
            return {str(k): v for k, v in data.items()}
        except json.JSONDecodeError:
            # Corrupted file, reset
            return {}

def save_data(data):
    """Save the tasks data to the JSON file."""
    with open(DATA_FILE, 'w') as f:
        json.dump(data, f, indent=4)

def get_date_data(date_str):
    """Get the tasks dictionary for a specific date string (YYYY-MM-DD).
    If the date does not exist, initialize with all tasks set to False.
    """
    data = load_data()
    if date_str not in data:
        # Initialize
        data[date_str] = {task: False for task in TASKS}
        save_data(data)
    return data[date_str]

def update_date_data(date_str, statuses):
    """Update the tasks statuses for a given date string.
    `statuses` is a dict mapping task names to bool.
    """
    data = load_data()
    data[date_str] = statuses
    save_data(data)

def generate_month_view(year, month):
    """Generate data for rendering the monthly overview.
    Returns a list of days with date strings, weekday, and task statuses.
    """
    # First day of month
    first_day = datetime.date(year, month, 1)
    # Last day
    if month == 12:
        last_day = datetime.date(year, month, 31)
    else:
        last_day = datetime.date(year, month + 1, 1) - datetime.timedelta(days=1)
    num_days = last_day.day

    data = load_data()
    days = []
    for day in range(1, num_days + 1):
        date_obj = datetime.date(year, month, day)
        date_str = date_obj.isoformat()
        # Ensure data exists
        if date_str not in data:
            data[date_str] = {task: False for task in TASKS}
        day_info = {
            'date': date_str,
            'day': day,
            'weekday': date_obj.strftime('%a'),
            'statuses': data[date_str]
        }
        days.append(day_info)
    # Persist any new dates
    save_data(data)
    return days

@app.context_processor
def inject_now():
    """Provide the current year to templates for the footer."""
    return {'current_year': datetime.datetime.utcnow().year}

@app.route('/')
@app.route('/monthly')
def monthly():
    """Render the monthly view for an arbitrary month/year."""
    """If no `year`/`month` is supplied the current month is used."""

    """ Resolve the requested month & year """
    today = datetime.date.today()
    month = int(request.args.get('month', today.month))
    year  = int(request.args.get('year', today.year))

    """ Build the calendar and calculate prev/next """
    days = generate_month_view(year, month)

    prev_year, prev_month = get_previous_month(year, month)
    next_year, next_month = get_next_month(year, month)

    """ Render """
    return render_template(
        'monthly.html',
        year=year,
        month=month,
        month_name=datetime.date(year, month, 1).strftime('%B'),
        days=days,
        tasks=TASKS,
        # The four values below are used by the navigation links
        prev_year=prev_year, prev_month=prev_month,
        next_year=next_year, next_month=next_month
    )


@app.route('/daily', methods=['GET', 'POST'])
def daily():
    """Render and handle the daily overview page."""
    # Determine date: from query param or today's date
    date_str = request.args.get('date')
    if not date_str:
        date_str = datetime.date.today().isoformat()

    # Validate date format
    try:
        date_obj = datetime.datetime.strptime(date_str, '%Y-%m-%d').date()
    except ValueError:
        # Invalid date, redirect to monthly
        return redirect(url_for('monthly'))

    if request.method == 'POST':
        # Process form submission
        statuses = {}
        for idx, task in enumerate(TASKS):
            key = f'task_{idx}'
            statuses[task] = key in request.form
        update_date_data(date_str, statuses)
        return redirect(url_for('daily', date=date_str))

    # For GET request, load data
    day_statuses = get_date_data(date_str)
    return render_template(
        'daily.html',
        date=date_str,
        tasks=TASKS,
        statuses=day_statuses
    )

if __name__ == '__main__':
    # Host 0.0.0.0 to allow access from other devices on the LAN
    app.run(host='0.0.0.0', port=5000, debug=True)



FILE: requirements.txt

Flask>=2.2


FILE: README.md
# Personal Task Tracker

A lightweight local web application for tracking daily tasks with a monthly overview.

## Features

- **Daily Overview** – View and toggle tasks for any chosen day.
- **Monthly Overview** – See a calendar grid with completion status for each task per day.
- **Local Server** – Host on your local network so any device on the same LAN can view the site.
- **Persistent Storage** – Tasks are saved in a JSON file (`tasks.json`).

## Setup

### Prerequisites

- Python 3.8+ (or any recent Python 3.x)
- `pip` (Python package installer)

### Installation

```bash
# Clone the repository or copy the folder structure locally
cd path/to/personal-task-tracker

# (Optional) Create a virtual environment
python -m venv venv
source venv/bin/activate   # On Windows: venv\Scripts\activate

# Install dependencies
pip install -r requirements.txt

# Running the server
python app.py

FILE: templates/base.html

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Task Tracker{% endblock %}</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='css/style.css') }}">
</head>

<body>
    <header>
        <h1>Daily Task Tracker</h1>
        <nav>
            <a href="{{ url_for('monthly') }}">Monthly Overview</a>
        </nav>

        <!-- ──────────────────────────────────────────────────────── -->
        <!-- Theme switch (same on every page) -->
        <!-- ──────────────────────────────────────────────────────── -->
        <div class="theme-switch">
            <input type="checkbox" id="theme-toggle" aria-label="Toggle light/dark mode">
            <label for="theme-toggle"></label>
            <!-- Icon that changes when the theme changes -->
            <span id="theme-label" aria-hidden="true"></span>
        </div>
    </header>

    <main>
        {% block content %}{% endblock %}
    </main>

    <footer>
        <p>&copy; {{ current_year }} $sudo</p>
    </footer>

    <!-- ──────────────────────────────────────────────────────── -->
    <!-- Load the JS that powers the switch -->
    <!-- ──────────────────────────────────────────────────────── -->
    <script src="{{ url_for('static', filename='js/theme.js') }}"></script>
</body>
</html>



FILE: templates/daily.html

{% extends "base.html" %}
{% block title %}Daily Overview - {{ date }}{% endblock %}
{% block content %}
<h2>Daily Overview - {{ date }}</h2>

<form method="get" action="{{ url_for('daily') }}">
    <label for="date-select">Select date:</label>
    <input type="date" id="date-select" name="date" value="{{ date }}" onchange="this.form.submit()">
</form>

<form method="post" action="{{ url_for('daily', date=date) }}">
    <table class="task-table">
        <thead>
            <tr>
                <th>Task</th>
                <th>Completed</th>
            </tr>
        </thead>
        <tbody>
            {% for task in tasks %}
                <tr>
                    <td>{{ task }}</td>
                    <td>
                        <!-- Use loop.index0 as the zero‑based index -->
                        <input type="checkbox"
                               name="task_{{ loop.index0 }}"
                               id="task_{{ loop.index0 }}"
                               {% if statuses[task] %}checked{% endif %}>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
    <button type="submit">Save</button>
</form>

<p><a href="{{ url_for('monthly') }}">Back to Monthly Overview</a></p>
{% endblock %}


FILE: templates/monthly.html

{% extends "base.html" %}
{% block title %}Monthly Overview - {{ month_name }} {{ year }}{% endblock %}
{% block content %}

<!-- ── Navigation ───────────────────────────────────── -->
<div class="month-navigation">
    <a href="{{ url_for('monthly', month=prev_month, year=prev_year) }}"
       class="nav-btn nav-prev">← Previous Month</a>

    <h2>{{ month_name }} {{ year }}</h2>

    <a href="{{ url_for('monthly', month=next_month, year=next_year) }}"
       class="nav-btn nav-next">Next Month →</a>
</div>

<!-- ── Calendar table ──────────────────────────────────── -->
<h2>Monthly Overview - {{ month_name }} {{ year }}</h2>
<table class="calendar">
    <thead>
        <tr>
            <th>Task</th>
            {% for day in days %}
                <th>
                    <a href="{{ url_for('daily', date=day.date) }}">
                        {{ day.day }}<br>
                        {{ day.weekday }}
                    </a>
                </th>
            {% endfor %}
        </tr>
    </thead>
    <tbody>
        {% for task in tasks %}
            <tr>
                <td class="task-name">{{ task }}</td>
                {% for day in days %}
                    {% set done = day.statuses[task] %}
                    <td class="task-cell {% if done %}completed{% else %}incomplete{% endif %}">
                        {% if done %}
                            &#10004;
                        {% else %}
                            &#10006;
                        {% endif %}
                    </td>
                {% endfor %}
            </tr>
        {% endfor %}
    </tbody>
</table>
{% endblock %}



FILE: static/css/style.css
/* ──────────────────────────────────────────────────────── */
/*      Light‑theme defaults (CSS variables)                */
/* ──────────────────────────────────────────────────────── */
:root {
  /* Core colours */
  --bg:          #ffffff;
  --text:        #222222;

  /* Header / Footer */
  --header-bg:   #f5f5f5;
  --header-text: #222222;
  --link:        #0066cc;
  --link-hover:  #004488;

  /* Tables & grids */
  --table-border:          #ddd;
  --completed-bg:          #28a745;
  --completed-text:        #fff;
  --incomplete-bg:         #dc3545;
  --incomplete-text:       #fff;

  /* Buttons */
  --button-bg:   #007bff;
  --button-text: #fff;
  --button-hover: #0056b3;

  /* Inputs */
  --input-bg:    #f0f0f0;
  --input-text:  #222222;
  --input-border:#ccc;
}

/* ──────────────────────────────────────────────────────── */
/*     Dark‑theme overrides – use data‑attribute            */
/* ──────────────────────────────────────────────────────── */
[data-theme="dark"] {
  --bg:          #121212;
  --text:        #e0e0e0;

  --header-bg:   #1b1b1b;
  --header-text: #f0f0f0;
  --link:        #90caf9;
  --link-hover:  #64b5f6;

  --table-border:          #333;
  --completed-bg:          #28a745;
  --completed-text:        #fff;
  --incomplete-bg:         #dc3545;
  --incomplete-text:       #fff;

  --button-bg:   #007bff;
  --button-text: #fff;
  --button-hover: #0056b3;

  --input-bg:    #1e1e1e;
  --input-text:  #e0e0e0;
  --input-border:#444;
}

/* ──────────────────────────────────────────────────────── */
/*       Apply variables to the whole document              */
/* ──────────────────────────────────────────────────────── */
body {
  margin: 0;
  font-family: Arial, Helvetica, sans-serif;
  background-color: var(--bg);
  color: var(--text);
}

/* Header / Footer */
header, footer {
  background-color: var(--header-bg);
  color: var(--header-text);
}
header h1 { margin: 0; font-size: 1.5em; }
nav a {
  color: var(--link);
  margin-left: 15px;
  text-decoration: none;
  font-weight: bold;
}
nav a:hover { text-decoration: underline; color: var(--link-hover); }

/* Tables (shared) */
table { width: 100%; border-collapse: collapse; margin-top: 20px; }
table th, table td {
  border: 1px solid var(--table-border);
  padding: 5px;
  text-align: center;
  color: var(--text);
}
table th a { color: var(--text); text-decoration: none; }
table th a:hover { text-decoration: underline; }

/* Monthly grid – green for ✓, red for ✗ */
.task-cell.completed { background-color: var(--completed-bg); color: var(--completed-text); font-weight: bold; }
.task-cell.incomplete { background-color: var(--incomplete-bg); color: var(--incomplete-text); font-weight: bold; }

/* Daily‑overview form area */
.task-table { background-color: var(--header-bg); }
.task-table th, .task-table td {
  border: 1px solid var(--table-border);
  padding: 8px;
  text-align: left;
}

/* Buttons */
button {
  padding: 8px 12px;
  background-color: var(--button-bg);
  color: var(--button-text);
  border: none;
  border-radius: 4px;
  cursor: pointer;
  margin-top: 10px;
  transition: background-color 0.2s;
}
button:hover { background-color: var(--button-hover); }

/* Misc helpers */
input[type="date"] {
  padding: 5px;
  margin-left: 10px;
  background-color: var(--input-bg);
  color: var(--input-text);
  border: 1px solid var(--input-border);
  border-radius: 3px;
}
input[type="checkbox"] { transform: scale(1.2); }

/* Main padding */
main { padding: 20px; }

/* Navigation for the monthly view */
.month-navigation {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20px;
}
.month-navigation .nav-btn {
  background-color: var(--button-bg);
  color: var(--button-text);
  text-decoration: none;
  padding: 6px 12px;
  border-radius: 4px;
  font-size: 0.9rem;
  transition: background-color 0.2s;
}
.month-navigation .nav-btn:hover { background-color: var(--button-hover); }

/* ──────────────────────────────────────────────────────── */
/*       Theme‑switch styling (checkbox → toggle)           */
/* ──────────────────────────────────────────────────────── */
.theme-switch {
    display: flex;
    align-items: center;
    margin-left: 20px;        /* space after the nav link */
}

/* Hide the actual checkbox – it’s only the state keeper */
.theme-switch input[type="checkbox"] {
    display: none;
}

/* The track that is visible */
.theme-switch label {
    position: relative;
    display: inline-block;     /* makes width/height work */
    width: 48px;
    height: 24px;
    background: #ccc;          /* default “light” colour */
    border-radius: 12px;
    cursor: pointer;
    transition: background 0.3s;
    border: 1px solid #999;    /* optional subtle border */
}

/* The knob */
.theme-switch label::before {
    content: '';
    position: absolute;
    width: 22px;
    height: 22px;
    left: 1px;
    top: 1px;
    background: #fff;
    border-radius: 50%;
    transition: transform 0.3s;
}

/* Checked state – green background, knob slides right */
.theme-switch input:checked + label {
    background: #4cd964;
    border-color: #4cd964;
}
.theme-switch input:checked + label::before {
    transform: translateX(24px);
}

/* Optional – icon next to the switch (sun/moon) */
.theme-switch span {
    margin-left: 8px;
    font-size: 0.9rem;
    color: var(--text);
}


FILE: static/js/theme.js
/* static/js/theme.js – light/dark theme toggle */

(() => {
    const themeKey = 'theme';
    const body = document.body;
    const toggle = document.getElementById('theme-toggle');
    const icon  = document.getElementById('theme-label');

    // Apply the theme (sets data-theme, checkbox state, and icon)
    const applyTheme = theme => {
        body.setAttribute('data-theme', theme);
        if (toggle) toggle.checked = (theme === 'dark');
        if (icon)   icon.textContent = theme === 'dark' ? '🌙' : '☀️';
    };

    /* ── Initialise theme on page load ─────────────────────── */
    const stored = localStorage.getItem(themeKey);
    const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
    let theme = stored ?? (prefersDark ? 'dark' : 'light');
    applyTheme(theme);

    /* ── Listen for toggle changes ─────────────────────────── */
    if (toggle) {
        toggle.addEventListener('change', () => {
            const newTheme = toggle.checked ? 'dark' : 'light';
            localStorage.setItem(themeKey, newTheme);
            applyTheme(newTheme);
        });
    }
})();



I want to add the support of this new ToDo list described at the very beginning of this prompt.  What software changes to which files need to be made to accomplish adding the ToDo list as described including all of the specific functionality outlined?  Give detailed and specific instructions on exactly what changes, additions, and deletions need to be made to the software.  If any new files need to be made, explain their purpose and where they fit in the file structure.  Produce professional and clean looking software that is free of any bugs, and produces a clean looking web page.  Take all the time that is necessary, do not stop until completion, and go over everything thoroughly, and go back to ensure everything is formatted cleanly, that the software is free of any bugs, and that it produces a very clean and professional looking website.